using System;

namespace UTB.Eshop.Infrastructure.Identity.Enums
{
    /// <summary>
    /// Enumeration for Roles in database (they must match)
    /// enum is not required but may be useful
    /// </summary>
    public enum Roles
    {
        Admin,
        Manager,
        Customer
    }
}
